#include <stdio.h>
//Help Needed
void revarray(char *p){
    int temp;
    int count=0;
    while (*p != '\0'){
        temp = input[count];
        count++;   
    }
}

int main(){
    char input[100];
    printf("Enter a string of integers");
    scanf("%s", input);
    //printf("%s", input);
}